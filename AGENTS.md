# AGENTS.md

## 1. 文档定位

本文件是 `usmp` 项目的唯一主规范，后续所有开发、文档编写、代码评审、联调、自测与交付活动均必须遵循本文件。

- 适用对象：前端开发、后端联调、测试人员、产品协作人员、AI 编码助手
- 规范优先级：用户明确指令 > `AGENTS.md` > `CLAUDE.md` > 其他说明文档
- 冲突处理规则：任何文档若与本文件冲突，一律以本文件为准
- 执行原则：本文件中的“必须”“禁止”“不得”“仅允许”均为强约束，不是建议

## 2. 项目事实基线

### 2.1 项目基础信息

- 项目名称：`usmp`
- 项目定位：云安全管理平台前端项目
- 当前技术栈：`Vue 3`、`TypeScript`、`Vite`、`Element Plus`、`Pinia`、`Vue Router`、`Axios`、`UnoCSS`
- 当前构建工具：`Vite 7`
- 当前包管理与运行要求：`Node >= 20.19.0`、`npm >= 8.19.0`

### 2.2 当前目录事实

当前项目已存在以下核心目录，后续开发必须在既有结构内扩展，不得擅自另起体系：

- `src/api`：接口定义，按业务域拆分模块
- `src/views`：页面级视图
- `src/components`：可复用公共组件
- `src/layout`：整体布局与框架壳层
- `src/router`：路由定义与装配
- `src/store/modules`：Pinia 状态模块
- `src/utils`：通用工具、请求封装、通用能力
- `src/directive`：自定义指令
- `src/plugins`：插件级能力封装
- `src/assets`：静态资源与全局样式
- `src/lang`：国际化资源
- `src/types`：共享类型定义

### 2.3 当前实现约束

- 当前仓库已存在未提交代码变更
- 当前阶段目标是建立统一开发规范，不是重构、修复或整理已有实现
- 本阶段禁止修改任何现有业务代码、页面、组件、样式、配置、资源文件

## 3. 当前阶段边界

### 3.1 允许事项

当前阶段仅允许执行以下动作：

- 新增或更新规范性文档
- 补充项目说明、目录职责、开发流程、验收标准
- 为后续需求编写方案、任务边界或评审依据

### 3.2 禁止事项

在未获得用户明确授权前，禁止执行以下动作：

- 修改 `src/` 下任何现有源码文件
- 修改 `.env.*`、`vite.config.ts`、`package.json`、`index.html`、静态资源等配置或构建文件
- 修复、重构、美化、整理当前已有代码
- 以“顺手优化”为由扩大改动范围
- 因实现便利擅自引入新依赖、新框架、新目录体系

### 3.3 变更准入规则

后续若进入正式开发阶段，必须满足以下前置条件：

1. 需求边界已经明确
2. 用户已明确授权可改动代码
3. 改动范围、目标目录、验收标准已定义
4. 不会覆盖或回退当前工作区中已有未提交改动

## 4. 开发总原则

后续任何开发任务都必须满足以下原则：

- 必须先确认需求，再确认边界，再实施
- 必须优先复用当前项目已有结构、工具链和实现模式
- 必须保持最小改动面、最小影响范围、最小引入成本
- 必须保证实现可说明、可验证、可回滚
- 必须遵守单一职责，避免页面、组件、状态、接口职责混杂
- 必须避免“先做再说”“先改出来看看”的无边界开发

## 5. 目录与架构规范

### 5.1 通用目录职责

#### `src/views`

- 仅放页面级视图
- 负责页面编排、数据组织、页面级交互协调
- 不得在页面文件中直接堆积通用组件逻辑
- 不得把通用工具函数、请求封装或全局状态逻辑写进页面

#### `src/components`

- 仅放可复用组件
- 组件必须边界清晰、职责单一、输入输出明确
- 通用组件不得耦合具体业务页面路径、具体菜单或临时字段结构
- 业务耦合极强且不可复用的内容不得伪装成通用组件

#### `src/api`

- 统一管理接口定义
- 按业务域组织，不得把多个无关模块混写在同一文件
- 接口类型应尽量与实现同域维护，避免分散失控
- 页面不得直接绕过 `src/api` 书写请求地址和请求细节

#### `src/store/modules`

- 仅存放全局共享状态
- 状态必须按领域拆分，不得建立“大而全”全局 store
- 页面私有临时状态不得放入全局 store
- store 不得承担视图渲染职责

#### `src/router`

- 统一管理静态路由、动态路由装配与路由元信息
- 路由配置必须与权限模型保持一致
- 不得在页面内部以临时逻辑替代统一路由治理

#### `src/utils`

- 存放通用工具与底层能力
- 工具函数必须通用、稳定、可复用
- 禁止把一次性业务逻辑包装成“工具函数”塞入该目录

#### `src/layout`

- 负责整体框架壳层、导航、侧栏、标签页等布局能力
- 不得把业务页面逻辑混入布局组件

#### `src/directive`

- 统一管理自定义指令
- 指令必须具备明确适用场景和可复用价值
- 禁止为单页面临时行为创建复杂指令

#### `src/plugins`

- 用于封装插件能力和全局注入能力
- 禁止将普通业务逻辑伪装成插件

### 5.2 新增文件归属规则

- 新增页面放入 `src/views/<业务域>/`
- 新增接口放入 `src/api/<业务域>/`
- 新增 Pinia 模块放入 `src/store/modules/`
- 新增通用组件放入 `src/components/<组件名>/`
- 新增共享类型优先就近维护；确需共享时再放入 `src/types`
- 新增工具函数仅在具备跨模块复用价值时放入 `src/utils`

### 5.3 命名规范

- 目录名、文件名、模块名必须表达真实职责
- 禁止使用 `temp`、`test2`、`new`、`final`、`copy` 等无语义命名
- 页面、组件、store、API 命名必须在同一业务域内保持统一
- 同一概念只允许一种命名方式，禁止中英文、缩写、别名混用

## 6. 代码质量规范

### 6.1 TypeScript 规范

- 必须优先使用明确类型
- 禁止无边界滥用 `any`
- 允许使用 `any` 的前提是：确实无法在当前上下文收敛类型，且必须有清晰理由
- 类型命名必须与业务语义一致，不得使用模糊命名
- 公共接口、组件输入输出、store 结构应优先定义清晰类型

### 6.2 Vue 组件规范

- 页面负责组织页面，组件负责复用能力
- 单文件过大、职责过多时，必须拆分
- `template`、`script`、样式定义必须保持清晰分层
- 禁止把大量业务规则直接硬编码在模板表达式中
- 禁止通过复制粘贴制造多个近似组件

### 6.3 状态管理规范

- Pinia 只管理跨页面、跨组件共享状态
- 页面筛选条件、弹窗开关、临时表单值等局部状态，默认留在页面或局部组件中
- store 的 action 必须表达业务意图，不得沦为杂项函数集合
- 不得在多个 store 间制造循环依赖和隐式耦合

### 6.4 接口与请求规范

- 所有 HTTP 请求必须统一走 `src/utils/request.ts`
- 不得重复创建第二套请求封装
- 接口定义必须放在 `src/api` 域内
- 请求头加密、重复提交控制、登录态处理必须遵循现有请求体系
- 禁止在页面中直接拼接复杂请求逻辑或重复处理通用错误

### 6.5 样式规范

- 必须优先复用现有样式体系与项目变量
- 禁止为追求局部效果破坏全局样式一致性
- 禁止大量堆积行内样式
- 禁止出现无法解释来源的魔法数和随意覆盖
- 样式实现必须兼顾已有布局结构和组件生态

### 6.6 注释与文档规范

- 注释只解释必要背景、边界和复杂原因
- 禁止编写“代码做了什么”式废话注释
- 注释、文档、命名必须与实现一致
- 代码改动涉及规则变化时，相关文档必须同步更新

## 7. 禁止事项清单

后续开发中，以下行为一律禁止：

- 未经明确授权修改现有代码
- 未经评估擅自重构现有模块
- 未经批准引入新依赖替代已有能力
- 绕开现有路由、权限、请求、状态管理体系另起一套实现
- 提交调试代码、日志垃圾、占位实现、注释掉的大段旧代码
- 提交未使用文件、未使用变量、未使用组件、未使用接口
- 在文档、代码、示例配置中写入密钥、口令、令牌、真实敏感地址
- 需求未确认即直接进入实现
- 没有验收标准就声称“已完成”
- 借单个需求夹带无关优化、无关修复或样式重做

## 8. 文档与协作规范

### 8.1 文档要求

- 文档内容以中文为主
- 文档必须直接可执行、可引用、可落地
- 规范文档不得写成空泛口号
- 所有边界、禁止事项、验收标准必须写清楚

### 8.2 协作要求

- 提需求时必须说明目标、范围、限制、验收方式
- 实施前必须确认是否允许改动现有代码
- 涉及跨模块改动时，必须先说明影响范围
- 发现当前实现与需求冲突时，必须先暴露问题，不得擅自决定

### 8.3 文档治理要求

- `AGENTS.md` 是主规范，后续规范更新优先修改本文件
- `CLAUDE.md` 仅作为项目入口说明，不承载第二套规则
- 若项目阶段、技术栈或协作方式发生变化，必须同步更新本文件

## 9. 标准执行流程

后续任务默认按以下流程执行：

1. 需求确认
2. 范围确认
3. 边界确认
4. 方案说明
5. 用户批准
6. 实施
7. 自检
8. 验收说明

执行要求如下：

- 未完成前 5 步，不得进入代码实施
- 未经用户批准，不得扩大范围
- 实施后必须说明改动内容、影响范围、验证方式和未覆盖风险

## 10. 验收标准

后续所有任务的完成结论，至少必须满足以下验收项：

### 10.1 通用验收标准

- 需求目标明确且已实现
- 改动范围与授权范围一致
- 未误改无关代码、配置、资源文件
- 命名、类型、结构、职责符合本规范
- 文档、说明、实现结果相互一致

### 10.2 代码质量验收标准

- 无明显职责混乱
- 无明显重复实现
- 无未解释的临时方案
- 无新增敏感信息泄露
- 无无意义注释、无未使用代码、无脏实现残留

### 10.3 交付说明验收标准

- 必须说明修改了什么
- 必须说明为什么这样改
- 必须说明有没有验证、如何验证
- 必须说明剩余风险或未覆盖项

### 10.4 当前阶段专项验收标准

当前阶段的交付结果，必须额外满足以下条件：

- 仅新增或更新文档
- 未改动现有业务代码
- `AGENTS.md` 已形成后续统一开发规范
- `CLAUDE.md` 已收敛为入口说明并引用 `AGENTS.md`

## 11. 后续开发准入补充

若后续新增能力超出当前项目基线，例如引入新的图表规范、流程设计器能力、复杂编辑器或新的状态管理约束，必须先补充对应规范并获得批准，再开始实施。

在新增专项规范前，不得默认：

- 新建第二套架构目录
- 推翻当前 Pinia、Router、Axios 请求体系
- 以未来规划为由重写当前项目结构

## 12. 结论

本项目后续开发必须以“边界明确、最小改动、质量可验、结果可追踪”为底线执行。未被明确授权的代码改动一律视为违规变更。
