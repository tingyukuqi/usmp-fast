# AGENTS.md

## 1. 文档定位

本文件是 `usmp` 项目的唯一主规范。后续所有开发、文档编写、代码评审、联调、自测与交付活动，均必须以本文件为准。

- 适用对象：前端开发、后端联调、测试人员、产品协作人员、AI 编码助手
- 规范优先级：用户明确指令 > `AGENTS.md` > `CLAUDE.md` > 其他说明文档
- 冲突处理规则：任何文档若与本文件冲突，一律以本文件为准
- 执行原则：本文件中的“必须”“禁止”“不得”“仅允许”均为强约束，不是建议
- 更新时间：`2026-04-12`

## 2. 项目事实基线

### 2.1 项目基础信息

- 项目名称：`usmp`
- 当前包名：`xxxxxx-云监管平台`
- 项目定位：云安全管理平台前端项目，当前实现基于 `xxxxxx-云监管平台` 前端体系
- 运行要求：`Node >= 20.19.0`、`npm >= 8.19.0`

### 2.2 当前技术栈事实

- `Vue 3.5.30`
- `TypeScript 5.9.3`
- `Vite 7.3.1`
- `Element Plus 2.13.5`
- `Pinia 3.0.4`
- `Vue Router 5.0.3`
- `Axios 1.13.6`
- `UnoCSS 66.6.6`
- `Vitest 4.0.18`
- `Sass 1.98.0`

### 2.3 当前源码目录事实

当前 `src/` 下已存在以下一级结构，后续开发必须优先在既有结构内扩展，不得擅自另起体系：

- `src/api`：接口定义与相关类型
- `src/assets`：静态资源与样式资源
- `src/components`：公共组件
- `src/directive`：自定义指令
- `src/enums`：枚举常量
- `src/hooks`：组合式复用逻辑
- `src/lang`：国际化资源
- `src/layout`：框架壳层与布局组件
- `src/plugins`：插件能力与全局能力封装
- `src/router`：路由定义
- `src/store`：Pinia 状态管理
- `src/types`：共享类型
- `src/utils`：通用工具与底层能力
- `src/views`：页面级视图

### 2.4 当前业务文件事实

截至本次校验，仓库中已确认存在以下关键实现形态：

- `src/api` 顶层已存在 `login.ts`、`menu.ts`、`types.ts`
- `src/api` 下已预留 `demo/`、`monitor/`、`system/`、`tool/`、`workflow/` 等业务目录
- `src/views` 当前已存在 `login.vue`、`register.vue`、`index.vue`、`error/401.vue`、`error/404.vue`、`redirect/index.vue`
- `src/store/modules` 当前已存在 `app`、`dict`、`notice`、`permission`、`settings`、`tagsView`、`user` 模块

### 2.5 当前文档目录事实

当前项目根目录下已存在以下文档产出目录：

- `docs/api/`：API 接口文档
- `docs/superpowers/plans/`：计划文档
- `docs/superpowers/specs/`：设计与规格文档
- `docs/superpowers/wave/`：计划执行过程分析文档
- `docs/supply/`：补充材料

### 2.6 当前实现基线

后续方案设计、接口定义、代码评审与实现说明，必须基于以下已确认事实，不得凭空假设另一套体系：

- 请求入口统一使用 `src/utils/request.ts`
- 当前请求体系已包含 `Authorization`、`Content-Language`、防重复提交、登录态失效处理、加解密与下载能力
- 路由入口位于 `src/router/index.ts`，采用静态路由 + 动态权限路由装配
- 路由守卫位于 `src/permission.ts`，通过用户信息与权限 store 注入动态路由
- 全局状态管理统一使用 `Pinia`
- 国际化能力位于 `src/lang`
- 自定义指令能力位于 `src/directive`

### 2.7 工作区状态原则

- 工作区是否存在未提交改动，必须以任务执行当下的 `git status` 结果为准
- 无论当前工作区是否干净，均不得覆盖、回退或破坏用户已有改动
- 不得在文档中写死“工作区必然干净”或“工作区必然有未提交改动”这类会过期的描述

## 3. 当前阶段边界

### 3.1 允许事项

当前阶段仅允许执行以下动作：

- 新增或更新规范性文档
- 补充项目说明、目录职责、开发流程、验收标准
- 为后续需求编写方案、任务边界、接口草案或评审依据

### 3.2 禁止事项

在未获得用户明确授权前，禁止执行以下动作：

- 修改 `src/` 下任何现有源码文件
- 修改 `.env.*`、`vite.config.ts`、`package.json`、`index.html`、静态资源等配置或构建文件
- 修复、重构、美化、整理当前已有代码
- 以“顺手优化”为由扩大改动范围
- 因实现便利擅自引入新依赖、新框架、新目录体系

### 3.3 变更准入规则

后续若进入正式开发阶段，必须满足以下前置条件：

1. 需求边界已经明确
2. 用户已明确授权可改动代码
3. 改动范围、目标目录、验收标准已定义
4. 不会覆盖或回退当前工作区中的已有改动

## 4. 文档产出规范

### 4.1 文档语言要求

- 所有新增或更新文档必须使用中文
- 规范文档必须直接可执行、可引用、可落地
- 禁止编写空泛口号式文档

### 4.2 文档落点要求

- 计划文档必须写入 `docs/superpowers/plans/`
- 设计文档必须写入 `docs/superpowers/specs/`
- 计划执行过程分析文档必须写入 `docs/superpowers/wave/`
- 正式编码前，必须优先输出可靠、无歧义的 API 接口文档到 `docs/api/`

### 4.3 文档治理要求

- `AGENTS.md` 是主规范，后续规范更新优先修改本文件
- `CLAUDE.md` 仅作为项目入口说明，不承载第二套规则
- 若项目阶段、技术栈、结构或协作方式发生变化，必须同步更新本文件

## 5. 开发总原则

后续任何开发任务都必须满足以下原则：

- 必须先确认需求，再确认边界，再实施
- 必须优先复用当前项目已有结构、工具链和实现模式
- 必须保持最小改动面、最小影响范围、最小引入成本
- 必须保证实现可说明、可验证、可回滚
- 必须遵守单一职责，避免页面、组件、状态、接口职责混杂
- 必须避免“先做再说”“先改出来看看”的无边界开发
- 遇到歧义时，必须先澄清，不得以猜测替代需求

## 6. 目录与架构规范

### 6.1 `src/views`

- 仅放页面级视图
- 负责页面编排、数据组织、页面级交互协调
- 不得在页面文件中堆积通用组件逻辑、请求封装或全局状态逻辑

### 6.2 `src/components`

- 仅放可复用组件
- 组件必须边界清晰、职责单一、输入输出明确
- 通用组件不得耦合具体业务页面路径、具体菜单或临时字段结构

### 6.3 `src/api`

- 统一管理接口定义与接口相关类型
- 页面不得绕过 `src/api` 直接书写请求地址和请求细节
- 新增接口应优先按业务域归档；若无合适业务域，再补充目录后落位
- 不得把多个无关模块长期混写在同一文件中

### 6.4 `src/store/modules`

- 仅存放跨页面、跨组件共享状态
- 状态必须按领域拆分，不得建立“大而全” store
- 页面私有临时状态默认留在页面或局部组件中
- store action 必须表达明确业务意图

### 6.5 `src/router`

- 统一管理静态路由、动态路由装配与路由元信息
- 路由配置必须与权限模型一致
- 不得在页面内部用临时逻辑替代统一路由治理

### 6.6 `src/utils`

- 存放通用工具与底层能力
- 工具函数必须通用、稳定、可复用
- 禁止把一次性业务逻辑包装成“工具函数”塞入该目录

### 6.7 `src/layout`

- 负责整体框架壳层、导航、侧栏、标签页等布局能力
- 不得把业务页面逻辑混入布局组件

### 6.8 `src/directive`

- 统一管理自定义指令
- 指令必须具备明确适用场景和复用价值
- 禁止为单页面临时行为创建复杂指令

### 6.9 `src/plugins`

- 用于封装插件能力和全局注入能力
- 禁止将普通业务逻辑伪装成插件

### 6.10 `src/hooks`

- 仅放组合式复用逻辑
- hook 必须抽象明确，避免夹带页面专属耦合
- 不得将副作用失控的流程直接封装为通用 hook

### 6.11 `src/enums`

- 仅放共享枚举、状态码、常量映射
- 命名必须表达清晰语义，不得制造来源不明的魔法常量

### 6.12 `src/types`

- 用于共享类型定义
- 类型应优先就近维护，只有具备跨模块复用价值时才提升到该目录

### 6.13 新增文件归属规则

- 新增页面放入 `src/views/<业务域>/`
- 新增接口放入 `src/api/<业务域>/` 或现有同域文件
- 新增 Pinia 模块放入 `src/store/modules/`
- 新增通用组件放入 `src/components/<组件名>/`
- 新增组合式逻辑放入 `src/hooks/`
- 新增共享类型在具备复用价值时放入 `src/types/`
- 新增工具函数仅在具备跨模块复用价值时放入 `src/utils/`

### 6.14 命名规范

- 目录名、文件名、模块名必须表达真实职责
- 禁止使用 `temp`、`test2`、`new`、`final`、`copy` 等无语义命名
- 页面、组件、store、API 命名必须在同一业务域内保持统一
- 同一概念只允许一种命名方式，禁止中英文、缩写、别名混用

## 7. 代码与实现规范

### 7.1 TypeScript 规范

- 必须优先使用明确类型
- 禁止无边界滥用 `any`
- 允许使用 `any` 的前提是确实无法在当前上下文收敛类型，且必须有清晰理由
- 类型命名必须与业务语义一致，不得使用模糊命名

### 7.2 Vue 组件规范

- 页面负责组织页面，组件负责复用能力
- 单文件过大、职责过多时，必须拆分
- `template`、`script`、样式定义必须保持清晰分层
- 禁止把大量业务规则直接硬编码在模板表达式中

### 7.3 状态管理规范

- Pinia 只管理共享状态
- 页面筛选条件、弹窗开关、临时表单值等局部状态，默认留在页面或局部组件中
- 不得在多个 store 间制造循环依赖和隐式耦合

### 7.4 接口与请求规范

- 所有 HTTP 请求必须统一走 `src/utils/request.ts`
- 不得重复创建第二套请求封装
- 请求鉴权、国际化、重复提交控制、登录态处理、下载与加解密能力必须遵循现有请求体系
- 禁止在页面中直接拼接复杂请求逻辑或重复处理通用错误

### 7.5 路由与权限规范

- 路由定义必须由 `src/router` 统一管理
- 权限获取、动态路由注入与登录态守卫必须沿用现有权限链路
- 禁止在页面组件内临时追加权限判断分支替代统一治理

### 7.6 样式规范

- 必须优先复用现有样式体系与项目变量
- 禁止为追求局部效果破坏全局样式一致性
- 禁止大量堆积行内样式
- 禁止出现无法解释来源的魔法数和随意覆盖

### 7.7 注释与文档规范

- 注释只解释必要背景、边界和复杂原因
- 禁止编写“代码做了什么”式废话注释
- 注释、文档、命名必须与实现一致
- 规则变化时，相关文档必须同步更新

## 8. 禁止事项清单

后续开发中，以下行为一律禁止：

- 未经明确授权修改现有代码
- 未经评估擅自重构现有模块
- 未经批准引入新依赖替代已有能力
- 绕开现有路由、权限、请求、状态管理体系另起一套实现
- 提交调试代码、日志垃圾、占位实现、注释掉的大段旧代码
- 提交未使用文件、未使用变量、未使用组件、未使用接口
- 在文档、代码、示例配置中写入密钥、口令、令牌、真实敏感地址
- 需求未确认即直接进入实现
- 没有验收标准就声称“已完成”
- 借单个需求夹带无关优化、无关修复或样式重做

## 9. 标准执行流程

后续任务默认按以下流程执行：

1. 需求确认
2. 范围确认
3. 边界确认
4. 方案说明
5. 用户批准
6. 文档先行
7. 实施
8. 自检
9. 验收说明

执行要求如下：

- 未完成前 5 步，不得进入代码实施
- 涉及编码任务时，原则上必须先完成 API 文档或方案文档
- 未经用户批准，不得扩大范围
- 实施后必须说明改动内容、影响范围、验证方式和未覆盖风险

## 10. 验收标准

后续所有任务的完成结论，至少必须满足以下验收项：

### 10.1 通用验收标准

- 需求目标明确且已实现
- 改动范围与授权范围一致
- 未误改无关代码、配置、资源文件
- 命名、类型、结构、职责符合本规范
- 文档、说明、实现结果相互一致

### 10.2 代码质量验收标准

- 无明显职责混乱
- 无明显重复实现
- 无未解释的临时方案
- 无新增敏感信息泄露
- 无无意义注释、无未使用代码、无脏实现残留

### 10.3 交付说明验收标准

- 必须说明修改了什么
- 必须说明为什么这样改
- 必须说明有没有验证、如何验证
- 必须说明剩余风险或未覆盖项

### 10.4 当前阶段专项验收标准

当前阶段的交付结果，必须额外满足以下条件：

- 仅新增或更新文档
- 未改动现有业务代码
- `AGENTS.md` 已形成后续统一开发规范
- 新增规范已明确文档落点、流程边界与验收口径

## 11. 后续开发准入补充

若后续新增能力超出当前项目基线，例如引入新的图表规范、流程设计器能力、复杂编辑器或新的状态管理约束，必须先补充对应规范并获得批准，再开始实施。

在新增专项规范前，不得默认：

- 新建第二套架构目录
- 推翻当前 Pinia、Router、Axios 请求体系
- 以未来规划为由重写当前项目结构

## 12. 结论

本项目后续开发必须以“边界明确、最小改动、质量可验、结果可追踪”为底线执行。未被明确授权的代码改动，一律视为违规变更。
